.venv
# video2text.app/ is entirely build output (./build.sh) — its source lives
# in packaging/ instead. The built app is distributed via GitHub Releases,
# never committed here.
video2text.app/
.env
sample/
__pycache__/
*.pyc
.DS_Store

# Claude Code / MoAI-ADK tooling config (not part of the video2text app)
.claude/
.moai/
.mcp.json
.git_hooks/
.worktreeinclude
.claudeignore
CLAUDE.md
.github/
